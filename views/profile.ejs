<!DOCTYPE html>
<html lang="en">

<head>
    <title>Profile Page</title>
</head>

<body>
    <h1>Welcome <%= user.name %>
    </h1>
    <h3>Email: <%= user.email %>
    </h3>
    <h3>City: <%= user.city%>
    </h3>
    <ul>
        <%- include('common/header.ejs'); %>
            <% user.skills.forEach((item)=>{ %>
                <li>
                    <%= item %>
                </li>
                <% }) %>
                    </h3>
</body>

</html>